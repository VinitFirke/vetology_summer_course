"""Uncertainty quantification for the radiology classifier.

Sampling (uq_main.py) and analysis (uq_analyze_main.py) are separate programs with
a JSONL file between them, so the expensive step runs once and the analysis re-runs free.
"""
